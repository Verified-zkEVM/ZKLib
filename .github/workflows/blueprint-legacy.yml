# ======================================================================
# THIS FILE HAS BEEN REPLACED BY TWO SEPARATE WORKFLOWS:
# 1. blueprint-build.yml - For building the blueprint
# 2. website-deploy.yml - For building and deploying the website
#
# A backup of the original workflow is in backup-blueprint.yml
# ======================================================================

name: Workflow Replaced

on:
  # This workflow is disabled
  workflow_dispatch: 
    inputs:
      notice:
        description: 'This workflow is deprecated'
        required: false
        default: 'Please use blueprint-build.yml and website-deploy.yml instead'

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow has been replaced by blueprint-build.yml and website-deploy.yml"
          echo "See those files for the current implementation"
          exit 1 # Fail the workflow to make it clear it shouldn't be used
